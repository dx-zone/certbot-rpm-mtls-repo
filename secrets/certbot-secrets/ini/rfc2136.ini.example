# ---------------------------------------------------------------------------
# Certbot RFC2136 DNS Authenticator Credentials
# ---------------------------------------------------------------------------
# PURPOSE:
#   Enables Certbot to create and remove TXT records using DNS Dynamic Updates.
#
# REQUIREMENTS:
#   - DNS server must support RFC2136 (Dynamic Updates)
#   - TSIG authentication must be enabled
#
# SECURITY:
#   - This file contains DNS write credentials
#   - MUST be owned by root and have permissions: chmod 600
# ---------------------------------------------------------------------------


# ---------------------------------------------------------------------------
# DNS SERVER SETTINGS
# ---------------------------------------------------------------------------

# IP address of your DNS server that supports RFC2136 Dynamic Updates.
# NOTE:
#   - Must be an IPv4 or IPv6 address
#   - Hostnames are NOT supported by Certbot
#
# Example:
#   dns_rfc2136_server = 192.168.0.10

dns_rfc2136_server = 192.168.0.10


# DNS service port.
# Standard DNS port is 53.
# Change only if your DNS server listens on a custom port.

dns_rfc2136_port = 53


# ---------------------------------------------------------------------------
# TSIG AUTHENTICATION
# ---------------------------------------------------------------------------

# TSIG key name.
# Must EXACTLY match the key name defined in your DNS server configuration.
#
# BIND example:
#   key "certbot-key" { ... }

dns_rfc2136_name = certbot-key


# Base64-encoded TSIG secret.
# Copy this value directly from your DNS server configuration.

dns_rfc2136_secret = BASE64_SECRET_GOES_HERE==


# TSIG algorithm used by your DNS server.
# Must match the algorithm configured server-side.
#
# Supported values:
#   - HMAC-MD5
#   - HMAC-SHA1
#   - HMAC-SHA224
#   - HMAC-SHA256   (Recommended)
#   - HMAC-SHA384
#   - HMAC-SHA512

dns_rfc2136_algorithm = HMAC-SHA256

